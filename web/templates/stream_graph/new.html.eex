<h2>New stream graph</h2>

<%= render "form.html", changeset: @changeset,
                        action: stream_graph_path(@conn, :create) %>

<%= link "Back", to: stream_graph_path(@conn, :index) %>
